name: Apple Trailer Manzana (v3.0-Precision)

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: '粘贴 Apple TV+ 网页 URL'
        required: true
      mode:
        type: choice
        description: '选择下载预设'
        options:
          - '4K_HDR_Best'
          - '1080p_SDR_Best'
          - 'Custom_IDs'
        default: '4K_HDR_Best'
      custom_sequence:
        description: '若选 Custom_IDs，请填入序列'
        required: false
        default: ''

jobs:
  manzana-job:
    runs-on: ubuntu-latest

    steps:
      - name: v3.0-Checkout
        uses: actions/checkout@v4

      - name: v3.0-Install Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg gpac

      - name: v3.0-Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: v3.0-Clone and Install
        run: |
          git clone https://github.com/dropcreations/Manzana-Apple-TV-Plus-Trailers.git
          cd Manzana-Apple-TV-Plus-Trailers
          pip install -r requirements.txt

      - name: v3.0-Execute Manzana
        run: |
          cd Manzana-Apple-TV-Plus-Trailers
          
          # 动态匹配输入序列
          if [ "${{ github.event.inputs.mode }}" == "4K_HDR_Best" ]; then
            INPUT_SEQ="0\n13\nall\nall\nall\nall\n"
          elif [ "${{ github.event.inputs.mode }}" == "1080p_SDR_Best" ]; then
            INPUT_SEQ="1\n11\nall\nall\nall\nall\n"
          else
            INPUT_SEQ="${{ github.event.inputs.custom_sequence }}"
          fi

          echo "🚀 开始精准下载..."
          printf "$INPUT_SEQ" | python manzana.py "${{ github.event.inputs.video_url }}" -d | grep -v "Fetching segments"

      - name: v3.0-Precision Extraction
        if: always()
        run: |
          mkdir -p final_delivery
          
          # 基于 v2.2 的审计发现，直接从工具自带的 output 目录提取
          SRC_DIR="./Manzana-Apple-TV-Plus-Trailers/output"
          
          if [ -d "$SRC_DIR" ]; then
            echo "📦 发现成品目录，正在转移文件..."
            mv "$SRC_DIR"/* ./final_delivery/
          else
            echo "❌ 错误：未发现目录 $SRC_DIR，执行保底搜索..."
            find ./Manzana-Apple-TV-Plus-Trailers -name "*.mp4" -exec mv {} ./final_delivery/ \;
          fi

      - name: v3.0-Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Apple_Trailer_v3.0_Final
          path: final_delivery/*
          retention-days: 1
