name: Apple Trailer Manzana (v5.5-Master)

on:
  workflow_dispatch:
    inputs:
      video_url:
        description: '粘贴 Apple TV+ 网页 URL'
        required: true
      action_type:
        type: choice
        description: '模式选择 (先探测 ID，后正式下载)'
        options:
          - 'Probe_All_Details'
          - 'Formal_Download'
        default: 'Probe_All_Details'
      custom_sequence:
        description: '【正式下载填入】例如: 2\n52\nall\n4,45\nall\n2,50\n'
        required: false
        default: ''

jobs:
  manzana-job:
    runs-on: ubuntu-latest

    steps:
      - name: Master-Checkout
        uses: actions/checkout@v4

      - name: Master-Setup Env
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg gpac
          git clone https://github.com/dropcreations/Manzana-Apple-TV-Plus-Trailers.git
          cd Manzana-Apple-TV-Plus-Trailers && pip install -r requirements.txt

      # ---------------- 模式 1：混合探测 (诱导全量音频/字幕) ----------------
      - name: Master-Hybrid Probe
        if: github.event.inputs.action_type == 'Probe_All_Details'
        run: |
          cd Manzana-Apple-TV-Plus-Trailers
          echo "🔍 正在进行混合扫描 (诱导全量 ID 表格)..."
          
          # 使用混合序列：0(SDR) -> 13(1080p) -> 其余全部 all
          # 这样能绕过 Range 报错，并强迫展示所有音频和字幕 ID
          (printf "0\n13\nall\nall\nall\nall\n" | python manzana.py "${{ github.event.inputs.video_url }}" -d 2>&1 | tee ../raw_full.log) & 
          
          # 监控日志，检测到开始下载时杀掉进程
          for i in {1..60}; do
            if grep -q "Downloading segments" ../raw_full.log; then
              echo "✅ 已捕获全量菜单，正在拦截下载..."
              pkill -f python
              break
            fi
            sleep 1
          done
          
          # 清理 ANSI 颜色代码并保存报告
          cd ..
          sed -r "s/\x1B\[([0-9]{1,3}(;[0-9]{1,2})?)?[mGK]//g" raw_full.log > Full_ID_Report.txt
          
          echo "--- 探测结果预览 ---"
          grep -E "│|╭|╰|ID|Codec|Bitrate|Language|Content|Range" Full_ID_Report.txt

      - name: Master-Upload Report
        if: github.event.inputs.action_type == 'Probe_All_Details'
        uses: actions/upload-artifact@v4
        with:
          name: Full_ID_Report
          path: Full_ID_Report.txt

      # ---------------- 模式 2：正式下载 (高精度交付) ----------------
      - name: Master-Execute Download
        if: github.event.inputs.action_type == 'Formal_Download'
        run: |
          cd Manzana-Apple-TV-Plus-Trailers
          echo "🚀 启动精准下载，序列: ${{ github.event.inputs.custom_sequence }}"
          printf "${{ github.event.inputs.custom_sequence }}" | python manzana.py "${{ github.event.inputs.video_url }}" -d | grep -v "Fetching segments"

      - name: Master-Extraction
        if: github.event.inputs.action_type == 'Formal_Download'
        run: |
          mkdir -p final_delivery
          # 优先提取工具默认输出目录
          if [ -d "./Manzana-Apple-TV-Plus-Trailers/output" ]; then
            mv ./Manzana-Apple-TV-Plus-Trailers/output/* ./final_delivery/
          fi
          # 保底提取根目录所有 mp4
          find ./Manzana-Apple-TV-Plus-Trailers -maxdepth 2 -name "*.mp4" -exec mv {} ./final_delivery/ \; 2>/dev/null || true

      - name: Master-Upload Result
        if: github.event.inputs.action_type == 'Formal_Download'
        uses: actions/upload-artifact@v4
        with:
          name: Apple_Video_Final
          path: final_delivery/*
          retention-days: 7
